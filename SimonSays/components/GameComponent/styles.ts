import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
   game: {
      backgroundColor: 'gray',
      //overflow: 'hidden',
      padding: 5,
      alignSelf: 'center',
   },
   score: {textAlign: 'center', color: 'red', fontSize: 20},
});

export default styles;
